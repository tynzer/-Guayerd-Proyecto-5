import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <div>
                estructura del header mas el navBar
            </div>
        );
    }
}

export default Header;